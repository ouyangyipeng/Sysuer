<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.中大儿" parent="@style/Theme.SYSUER.Light">
        <item name="dropdownPreferenceStyle">
            @style/preference
        </item>
        <item name="android:windowAnimationStyle">@style/fade</item>
    </style>

    <style name="Theme.SYSUER.Light" parent="Theme.Material3.Light.NoActionBar">
        <!--        <item name="preferenceFragmentStyle">-->
        <!--            @style/PF-->
        <!--        </item>-->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="preference" parent="@style/Preference.DropDown.Material">

    </style>

    <style name="spinner" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">

        </item>
    </style>

    <style name="fade">
        <item name="android:activityOpenEnterAnimation">
            @android:anim/fade_in
        </item>
        <item name="android:activityCloseEnterAnimation">
            @android:anim/fade_in
        </item>
        <item name="android:activityOpenExitAnimation">
            @android:anim/fade_out
        </item>
        <item name="android:activityCloseExitAnimation">
            @android:anim/fade_out
        </item>
    </style>

    <style name="Theme.中大儿.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.中大儿.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.中大儿.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- Base application theme. -->
    <style name="Base.Theme.中大儿" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="SYSU.AppBarLayout" parent="Widget.Material3.AppBarLayout">
        <item name="android:fitsSystemWindows">true</item>
        <item name="statusBarForeground">@macro/m3_comp_app_bar_container_color</item>
        <item name="android:outlineSpotShadowColor" tools:targetApi="28">@color/black</item>
        <item name="android:outlineAmbientShadowColor" tools:targetApi="28">@color/black</item>
    </style>
    <style name="SYSU.AppBarLayout.Solid" parent="Widget.Material3.AppBarLayout">
        <item name="android:fitsSystemWindows">true</item>
        <item name="statusBarForeground">@macro/m3_comp_app_bar_container_color</item>
        <item name="android:outlineSpotShadowColor" tools:targetApi="28">@color/black</item>
        <item name="android:outlineAmbientShadowColor" tools:targetApi="28">@color/black</item>
    </style>
    <style name="RoundRect">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">4dp</item>
    </style>
</resources>